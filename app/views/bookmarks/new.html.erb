<div class="container">
  <h1>New bookmark </h1>

  <%= simple_form_for [@list, @bookmark] do |f|%>
    <%= f.association :movie %>
    <%# <%= f.association :list, as: :check_boxes %>
    <%= f.input :comment %>
    <%= f.submit "Submit review", class: "btn btn-primary" %>
  <% end %>

</div>
